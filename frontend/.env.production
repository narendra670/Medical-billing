# Set on Vercel (Environment Variables) to your Render backend URL, e.g.:
# VITE_API_URL=https://your-app.onrender.com
#
# OR leave empty and use frontend/vercel.json rewrites to proxy /api to Render.
# Do NOT rely on the Vercel site URL alone — /api does not exist on static hosting.
VITE_API_URL=