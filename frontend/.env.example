# Copy to .env.local for local dev (optional; Vite proxy handles /api by default)

# Production (Vercel): set in Project → Settings → Environment Variables
# Your Render backend URL, no trailing slash
# VITE_API_URL=https://medical-store-api.onrender.com
